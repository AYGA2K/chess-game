{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (CMake)",
      "type": "codelldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/output",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build"
    }
  ]
}
